// closed taxonomy, additive-only within a major (ARCHITECTURE §7.2); 'link' kept for taxonomy
// completeness though the Link primitive is NOT shipped in Slice 1
export type ComponentRole = 'button' | 'link' | 'textInput' | 'checkbox' | 'select';
