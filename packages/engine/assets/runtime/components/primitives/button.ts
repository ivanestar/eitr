import { Component } from '../base/component';

/**
 * A clickable control — works on a `<button>`, an `<a role="button">`, or a `<div>`-based button.
 * The primary interaction is `click()` (inherited from {@link Component}).
 */
export class Button extends Component {}
